const { describe, it } = require('mocha');
const { assert } = require('chai');

import QuestionsList from './questions-list';

describe('Компонент - "questions-list"', () => {
    describe('подзаголовок подруппы тестов', () => {
        it('описание проверки', () => {});
    });

    describe('подзаголовок подруппы тестов', () => {
        it('описание проверки', () => {});
    });
});
